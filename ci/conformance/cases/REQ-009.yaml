# REQ-009 — rANS-8 entropy coder for TOP/B lists.

id: REQ-009
description: TOP and B lists compressed using 8-bit-state rANS; decode round-trip succeeds.
sad_section: "§B. Entropy coder — rANS"
evidence: "[Brief]; [Patent col. 7-8]; [Duda 2014 §3.3]"
evidence_paths:
  - codec/include/scc/rans.hpp
  - codec/src/common/rans.cpp
  - codec/tests/test_rans.cpp
test_command: "ctest --test-dir build -R 'rans8_' --output-on-failure"
pass_criteria_type: exit_code
pass_criteria_expected: 0
timeout_seconds: 60
blocking: true
